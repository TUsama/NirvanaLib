package deps

object LoomDeps {
    fun get(minecraft: String): List<VersionedDependency> {
        val deps = mutableListOf<VersionedDependency>()
        when (minecraft) {
            "1.20.1" -> {
            }
        }
        return deps
    }
}